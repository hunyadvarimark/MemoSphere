<UserControl x:Class="WPF.Views.NoteDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="800">
    <UserControl.InputBindings>
        <KeyBinding Key="S" 
                    Modifiers="Control" 
                    Command="{Binding SaveNoteCommand}" />
    </UserControl.InputBindings>
    <StackPanel Margin="20">
        <TextBlock Text="Új Jegyzet Rögzítése" FontWeight="Bold" FontSize="18" Margin="0,0,0,15"/>

        <Label Content="Cím:"/>
        <TextBox Text="{Binding NoteTitle, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10" 
                 Padding="5"/>

        <Label Content="Tartalom:"/>
        <TextBox Text="{Binding NoteContent, UpdateSourceTrigger=PropertyChanged}" 
                 Margin="0,0,0,10" 
                 Height="250" 
                 TextWrapping="Wrap" 
                 AcceptsReturn="True" 
                 VerticalScrollBarVisibility="Auto" 
                 Padding="5"/>

        <Button Content="Jegyzet Mentése" 
                Command="{Binding SaveNoteCommand}" 
                Padding="10" 
                Margin="0,10,0,10"/>
        <Button Content="Jegyzet Törlése" 
                Command="{Binding DeleteNoteCommand}" 
                Padding="10" 
                Margin="0,10,0,10"/>    

        <TextBlock Text="{Binding StatusMessage}" 
                   Foreground="DarkGreen" 
                   FontStyle="Italic"
                   Margin="0,10,0,0"/>

        <TextBlock Text="Mentés folyamatban..." 
                   Margin="5,10,5,0" 
                   FontWeight="Bold">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSaving}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>
    </StackPanel>
</UserControl>