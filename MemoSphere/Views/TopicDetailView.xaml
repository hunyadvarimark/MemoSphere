<UserControl x:Class="WPF.Views.TopicDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="300">
    <StackPanel>
        <TextBlock Text="Témakör cím (Topic Title):" FontWeight="Bold"/>
        <TextBox Text="{Binding TopicTitle, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Mentés" Command="{Binding SaveTopicCommand}" Margin="0,0,5,0"/>
            <Button Grid.Column="1" Content="Mégsem" Command="{Binding CancelCommand}" Margin="5,0,0,0"/>
        </Grid>
    </StackPanel>
</UserControl>