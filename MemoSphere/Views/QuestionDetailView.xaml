<UserControl x:Class="WPF.Views.QuestionDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Core.Enums;assembly=MemoSphere.Core"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500">
    <StackPanel Margin="20">
        <TextBlock Text="Kérdés Szerkesztése" FontWeight="Bold" FontSize="18" Margin="0,0,0,15"/>

        <Label Content="Kérdés szövege:"/>
        <TextBox Text="{Binding QuestionText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Padding="5"/>

        <Label Content="Típus:"/>
        <ComboBox SelectedItem="{Binding QuestionType}" Margin="0,0,0,10">
            <ComboBox.Items>
                <ComboBoxItem Content="MultipleChoice" Tag="{x:Static local:QuestionType.MultipleChoice}"/>
                <ComboBoxItem Content="TrueFalse" Tag="{x:Static local:QuestionType.TrueFalse}"/>
                <ComboBoxItem Content="Open" Tag="{x:Static local:QuestionType.ShortAnswer}"/>
            </ComboBox.Items>
        </ComboBox>

        <Label Content="Helyes válasz:"/>
        <TextBox Text="{Binding CorrectAnswer, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" Padding="5"/>

        <Label Content="Opciók (csak MultipleChoice esetén):"/>
        <ListBox ItemsSource="{Binding Options}" Margin="0,0,0,10">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBox Text="{Binding}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Button Content="Mentés" Command="{Binding SaveQuestionCommand}" Padding="10" Margin="0,10,0,10"/>

        <!-- ÚJ: Kérdésgenerálás gomb (teszteléshez) -->
        <Label Content="Kérdésgenerálás (teszt):"/>
        <ComboBox x:Name="GenTypeCombo" SelectedItem="{Binding GenQuestionType}" Margin="0,0,0,10">
            <ComboBox.Items>
                <ComboBoxItem Content="MultipleChoice" Tag="{x:Static local:QuestionType.MultipleChoice}"/>
                <ComboBoxItem Content="TrueFalse" Tag="{x:Static local:QuestionType.TrueFalse}"/>
                <ComboBoxItem Content="Open" Tag="{x:Static local:QuestionType.ShortAnswer}"/>
            </ComboBox.Items>
        </ComboBox>
        <Button Content="Generálj Kérdéseket" Command="{Binding GenerateQuestionsCommand}" CommandParameter="{Binding ElementName=GenTypeCombo, Path=SelectedItem.Tag}" Padding="10" Margin="0,10,0,10"/>
    </StackPanel>
</UserControl>